# Creating my portfolio page

## const StepList = () => {

1. Create graphical project:

   > Create mobile display -> Create desktop display
   > Save all colors + icons

2. Create website:

   > Build crucial components
   > Assemble website
   > Style components
   > Add animations -> js prefered
   > Add Javascript functions if needed

## }
